/* CrackProof(Htsysm) ユーザーランド無力化。正は micetools htsysm 相当 + nrs.exe 個別検証。
 * P6 TODO: Border Break で Htsysm がどこまで干渉するか確認し、最小の残存パッチを置く。
 * （これが「残存バイナリパッチの唯一領域」。それ以外は device emulation で無改変。） */
void crackproof_init(void) { /* TODO(P6) */ }
